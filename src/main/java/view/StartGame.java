package view;

import model.User;

public class StartGame {
    User userTurn = null;

    StartGame(User user1, User user2, User user3, User user4) {
        generateMap();

    }

    StartGame(User user1, User user2, User user3) {
        generateMap();
    }

    private void generateMap() {

    }
}
